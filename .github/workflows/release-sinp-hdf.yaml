name: "Auto package & release SINP HDF (conservative, debug build:dist)"

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

permissions:
  contents: write

env:
  ASSET_NAME: sinp_hdf.tar.gz
  TARGET_REPO: sinp-hdf/sinp_hdf_front

jobs:
  package-and-release:
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Checkout repo (full history & tags)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: true

      - name: Pre-build debug (repo state)
        run: |
          echo "GITHUB_REPOSITORY=$GITHUB_REPOSITORY"
          echo "GITHUB_REF=$GITHUB_REF"
          echo "Runner pwd: $(pwd)"
          echo "--- git status ---"
          git status --porcelain || true
          echo "--- top-level files ---"
          ls -la || true
          echo "--- apps/sinp_hdf listing (top) ---"
          ls -la apps/sinp_hdf || true
          echo "--- show package.json if present ---"
          if [ -f apps/sinp_hdf/package.json ]; then
            echo "package.json content (first 200 lines):"
            sed -n '1,200p' apps/sinp_hdf/package.json || true
          else
            echo "apps/sinp_hdf/package.json NOT FOUND"
          fi

      - name: Setup Node.js (debug)
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Node/npm versions (after setup)
        run: |
          echo "node version:"; node -v || true
          echo "npm version:"; npm -v || true
          echo "which node: $(which node || true)"

      - name: Install dependencies (verbose)
        working-directory: apps/sinp_hdf
        run: |
          set -x
          echo "Working dir: $(pwd)"
          echo "Listing before npm ci:"
          ls -la
          echo "package.json present: [ -f package.json ] ->" ; [ -f package.json ] && echo "yes" || echo "no"
          npm ci --prefer-offline --no-audit --progress=false

      - name: Build (explicit, verbose, force build:dist)
        working-directory: apps/sinp_hdf
        run: |
          set -x
          echo "--- ENV before build ---"
          echo "CI=$CI"
          echo "NODE_ENV=$NODE_ENV"
          echo "BUILD_TARGET=${BUILD_TARGET:-<not-set>}"
          echo "Forcing NODE_ENV=production and BUILD_TARGET=main for the build"
          export NODE_ENV=production
          export BUILD_TARGET=main
          echo "Run: npm run build:dist (explicit)"
          npm run build:dist

      - name: Post-build debug (list outputs)
        run: |
          echo "=== After build: listing apps/sinp_hdf (depth 4) ==="
          find apps/sinp_hdf -maxdepth 4 -type f -print || true
          echo "--- du summary ---"
          du -sh apps/sinp_hdf || true
          if [ -d "apps/sinp_hdf/dist" ]; then
            echo "--- dist exists: listing ---"
            ls -la apps/sinp_hdf/dist || true
            find apps/sinp_hdf/dist -maxdepth 4 -type f -print || true
            du -sh apps/sinp_hdf/dist || true
          fi

      - name: Create archive (prefer dist/sinp_hdf, then dist, then fallback)
        run: |
          set -euo pipefail
          echo "Selecting directory to archive..."
          C1="apps/sinp_hdf/dist/sinp_hdf"
          C2="apps/sinp_hdf/dist"
          C3="apps/sinp_hdf"
          SELECTED=""
          if [ -d "$C1" ] && find "$C1" -mindepth 1 -print -quit | grep -q .; then
            SELECTED="$C1"
          elif [ -d "$C2" ] && find "$C2" -mindepth 1 -print -quit | grep -q .; then
            SELECTED="$C2"
          elif [ -d "$C3" ] && find "$C3" -mindepth 1 -print -quit | grep -q .; then
            SELECTED="$C3"
          fi
          if [ -z "$SELECTED" ]; then
            echo "::error::No build output found in expected locations: $C1, $C2, $C3"
            echo "Full recursive listing (3 levels) of apps/sinp_hdf:"
            find apps/sinp_hdf -maxdepth 3 -type f -print || true
            exit 1
          fi
          echo "Selected to archive: ${SELECTED}"
          if [ "$SELECTED" = "$C1" ]; then
            tar -czf "$GITHUB_WORKSPACE/${{ env.ASSET_NAME }}" -C "apps/sinp_hdf/dist" "sinp_hdf"
          else
            tar -czf "$GITHUB_WORKSPACE/${{ env.ASSET_NAME }}" -C "${SELECTED}" .
          fi
          echo "Archive created: $GITHUB_WORKSPACE/${{ env.ASSET_NAME }}"
          ls -la "$GITHUB_WORKSPACE/${{ env.ASSET_NAME }}"

      - name: Determine new tag from package.json and existing tags
        id: compute_tag
        run: |
          set -euo pipefail
          echo "Reading version from apps/sinp_hdf/package.json"
          version=$(node -e "console.log(require('./apps/sinp_hdf/package.json').version)")
          version="${version#v}"
          echo "package.json version: $version"
          IFS='.' read -ra parts <<< "$version"
          if [ "${#parts[@]}" -lt 3 ]; then
            echo "ERROR: package.json version must be at least MAJOR.MINOR.PATCH"
            exit 1
          fi
          base="${parts[0]}.${parts[1]}.${parts[2]}"
          echo "Base (first 3 fields): $base"
          tags=$(git ls-remote --tags origin | awk '{print $2}' | sed 's@refs/tags/@@' | sed 's@\^{}@@')
          max=-1
          for t in $tags; do
            tclean="${t#v}"
            if [ "$tclean" = "$base" ]; then
              num=0
              if [ $num -gt $max ]; then max=$num; fi
            else
              if [[ "$tclean" =~ ^${base}\.([0-9]+)$ ]]; then
                num="${BASH_REMATCH[1]}"
                if [ "$num" -gt "$max" ]; then max="$num"; fi
              fi
            fi
          done
          if [ "${#parts[@]}" -ge 4 ]; then
            last="${parts[3]}"
            if ! [[ "$last" =~ ^[0-9]+$ ]]; then
              echo "ERROR: 4th component is not numeric: $last"
              exit 1
            fi
            newnum=$((last + 1))
            newtag="${base}.${newnum}"
          else
            if [ $max -lt 0 ]; then
              newnum=1
            else
              newnum=$((max + 1))
            fi
            newtag="${base}.${newnum}"
          fi
          echo "Calculated new tag: ${newtag}"
          echo "tag=${newtag}" >> "$GITHUB_OUTPUT"

      - name: Get default branch
        id: default_branch
        run: |
          # Method 1: Try to get from remote HEAD symbolic ref
          default_branch=$(git symbolic-ref refs/remotes/origin/HEAD | sed 's@refs/remotes/origin/@@' || echo "")
          
          # Method 2: Fallback - use the repository API
          if [ -z "$default_branch" ]; then
            default_branch=$(gh repo view --json defaultBranchRef --jq '.defaultBranchRef.name')
          fi
          
          # Method 3: Final fallback - check which branch exists
          if [ -z "$default_branch" ]; then
            if git rev-parse --verify origin/main >/dev/null 2>&1; then
              default_branch="main"
            elif git rev-parse --verify origin/master >/dev/null 2>&1; then
              default_branch="master"
            else
              default_branch="main"  # Default fallback
            fi
          fi
          
          echo "Detected default branch: ${default_branch}"
          echo "default_branch=${default_branch}" >> "$GITHUB_OUTPUT"

      - name: Create annotated git tag and push (in source repo)
        env:
          NEW_TAG: ${{ steps.compute_tag.outputs.tag }}
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          echo "Creating tag ${NEW_TAG} in source repository"
          git tag -a "${NEW_TAG}" -m "Release ${NEW_TAG} (created by CI run ${GITHUB_RUN_ID})"
          git push origin "refs/tags/${NEW_TAG}"

      - name: Authenticate gh CLI for target repo
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN_SINP_HDF }}
          TARGET_REPO: ${{ env.TARGET_REPO }}
        run: |
          set -euo pipefail
          if [ -z "${GH_TOKEN:-}" ]; then
            echo "::warning::secrets.GH_TOKEN_SINP_HDF is empty - cannot authenticate to target repo. gh commands may fail."
          else
            echo "Authenticating gh CLI with provided token..."
            echo "${GH_TOKEN}" | gh auth login --with-token
            echo "Verifying access to target repo ${TARGET_REPO}..."
            if ! gh repo view "${TARGET_REPO}" >/dev/null 2>&1; then
              echo "::error::Cannot access target repo ${TARGET_REPO} with the provided token. Check secret and token permissions (repo write)."
              gh repo view "${TARGET_REPO}" || true
              exit 1
            fi
            echo "gh authenticated and target repo reachable"
          fi

      - name: Create or update versioned release in target repo
        env:
          NEW_TAG: ${{ steps.compute_tag.outputs.tag }}
          ASSET_PATH: ${{ github.workspace }}/${{ env.ASSET_NAME }}
          DEFAULT_BRANCH: ${{ steps.default_branch.outputs.default_branch }}
          GH_TOKEN: ${{ secrets.GH_TOKEN_SINP_HDF }}
        run: |
          set -euo pipefail
          echo "Creating or updating release ${NEW_TAG} in target repo ${{ env.TARGET_REPO }}"
          echo "Asset path: ${ASSET_PATH}"
          if [ ! -f "${ASSET_PATH}" ]; then
            echo "::error::Asset file not found at ${ASSET_PATH}"
            exit 1
          fi
          
          if gh release view "${NEW_TAG}" --repo "${{ env.TARGET_REPO }}" >/dev/null 2>&1; then
            echo "Release ${NEW_TAG} exists in target repo - attempting upload"
            if gh release upload "${NEW_TAG}" "${ASSET_PATH}" --repo "${{ env.TARGET_REPO }}" --clobber; then
              echo "Uploaded asset to ${NEW_TAG} in target repo"
            else
              echo "::warning::Upload to ${NEW_TAG} failed (immutable or permission). No destructive action will be taken."
            fi
          else
            if gh release create "${NEW_TAG}" --repo "${{ env.TARGET_REPO }}" --target "${DEFAULT_BRANCH}" --title "${NEW_TAG}" --notes "Release ${NEW_TAG} (auto-created by CI from SINP-Haut-de-France/mviewer_bs, workflow run ${GITHUB_RUN_ID})" --assets "${ASSET_PATH}"; then
              echo "Release ${NEW_TAG} created with asset in target repo"
            else
              echo "::warning::Failed to create release ${NEW_TAG} in target repo. Attempting upload if release exists"
              if gh release upload "${NEW_TAG}" "${ASSET_PATH}" --repo "${{ env.TARGET_REPO }}" --clobber; then
                echo "Uploaded asset to ${NEW_TAG} in target repo"
              else
                echo "::warning::upload still failed. Manual intervention may be required."
              fi
            fi
          fi

      - name: Ensure persistent 'Latest' release exists and upload asset
        env:
          ASSET_PATH: ${{ github.workspace }}/${{ env.ASSET_NAME }}
          NEW_TAG: ${{ steps.compute_tag.outputs.tag }}
          DEFAULT_BRANCH: ${{ steps.default_branch.outputs.default_branch }}
          TARGET_REPO: ${{ env.TARGET_REPO }}
          GH_TOKEN: ${{ secrets.GH_TOKEN_SINP_HDF }}
        run: |
          set -euo pipefail
          echo "Ensuring 'Latest' release exists in target repo ${TARGET_REPO} and uploading asset"
          echo "Asset path: ${ASSET_PATH}"
          if [ ! -f "${ASSET_PATH}" ]; then
            echo "::error::Asset file not found at ${ASSET_PATH}"
            exit 1
          fi

          if gh release view "Latest" --repo "${TARGET_REPO}" >/dev/null 2>&1; then
            echo "Release 'Latest' exists - uploading asset with --clobber and updating release metadata"
            if gh release upload "Latest" "${ASSET_PATH}" --repo "${TARGET_REPO}" --clobber; then
              echo "Uploaded asset to 'Latest' (clobber)"
            else
              echo "::warning::Failed to upload asset to 'Latest' (check permissions or immutability)"
            fi
            # Update release notes/target to point to DEFAULT_BRANCH and mention the version
            gh release edit "Latest" --repo "${TARGET_REPO}" --target "${DEFAULT_BRANCH}" --notes "Latest release from ${NEW_TAG} (source: SINP-Haut-de-France/mviewer_bs, workflow run ${GITHUB_RUN_ID})" || true
          else
            echo "Release 'Latest' not found - creating it and uploading asset"
            if gh release create "Latest" --repo "${TARGET_REPO}" --target "${DEFAULT_BRANCH}" --title "Latest" --notes "Latest release from ${NEW_TAG} (source: SINP-Haut-de-France/mviewer_bs, workflow run ${GITHUB_RUN_ID})" --assets "${ASSET_PATH}"; then
              echo "Release 'Latest' created with asset"
            else
              echo "::warning::Failed to create 'Latest' release in target repo (check permissions)."
            fi
          fi

      - name: Print final URLs and summary
        env:
          NEW_TAG: ${{ steps.compute_tag.outputs.tag }}
          TARGET_REPO: ${{ env.TARGET_REPO }}
        run: |
          echo "✅ Source repo tag created: ${NEW_TAG}"
          echo "📌 Source repo URL: https://github.com/${GITHUB_REPOSITORY}/releases/tag/${NEW_TAG}"
          echo ""
          echo "✅ Target repo release created in: ${TARGET_REPO}"
          echo "📥 Versioned download: https://github.com/${TARGET_REPO}/releases/download/${NEW_TAG}/${{ env.ASSET_NAME }}"
          echo "📥 Latest download (tagged 'Latest'): https://github.com/${TARGET_REPO}/releases/download/Latest/${{ env.ASSET_NAME }}"